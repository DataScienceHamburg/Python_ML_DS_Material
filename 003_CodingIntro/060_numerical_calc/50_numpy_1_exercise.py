#%% packages
import numpy as np
# %% E1:
# - create a matrix with dimension 10x10
# - fill it with values from 0 to 99
# - write a function which takes a value as input and returns the number (if it is a square-number), returns 0 if it is not a square number
# - replace all values which are not square-numbers, with 0
